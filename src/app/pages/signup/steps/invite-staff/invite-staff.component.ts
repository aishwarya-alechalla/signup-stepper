import { Component } from '@angular/core';
import { IonicModule } from '@ionic/angular';

@Component({
  standalone: true,
  selector: 'app-invite-staff',
  imports: [IonicModule],
  template: `<h2>Invite Staff</h2>`
})
export class InviteStaffComponent {}
